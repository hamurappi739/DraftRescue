# Передача исходной нейронке: ревью DraftRescue

Дата ревью: 2026-09-03  
Рабочая копия: `обновленное`

## Что уже сделано

1. Проверена рабочая копия проекта и прочитаны канонические документы, правила приватности и пакет Phase 1.
2. Phase 0 ранее подтверждён на Windows x64 с .NET SDK 8.0.424: restore, build, tests и запуск Avalonia shell.
3. WP-1.1 реализован: content-free источник foreground WinEvent с bounded очередью, фильтром собственного процесса, нормализацией process identity и корректным unhook/dispose.
4. WP-1.2 реализован: focused UI Automation metadata source на отдельном MTA worker. Callback UIA только сигнализирует worker; обход провайдера и чтение метаданных выполняются на worker.
5. В UIA cache/query добавлены только разрешённые структурные поля: роль/тип, process/HWND, framework, editability/read-only/protected/enabled/focus/offscreen и bounded structural tokens. Чтение текста запрещено и не вызывается: нет `TextPatternRange.GetText`, `ValuePattern.Value`, clipboard, keyboard hooks или persistence.
6. Добавлены unit-тесты для bounded metadata, отсутствия content-полей, регистрации и idempotent disposal.
7. WP-1.3 реализован: Application coordinator объединяет foreground/focus streams, отбрасывает устаревшие source sequence, coalesce-ит одинаковый логический контекст, ведёт `ContextGeneration`, фильтрует собственный процесс и выдаёт `NoContext/Transient/Unsupported/CandidateMetadata`.
8. WP-1.4 частично подтверждён synthetic harness: 1000-event burst, newest-context switch, injected provider fault, 25 warmed start/stop cycles и 30-секундный soak; JSON evidence записан в `artifacts/phase1`.
9. Реальный Notepad probe повторён после initial и foreground-triggered metadata reconciliation. Результат `Pass`: foreground для probe PID пришёл дважды, focused UIA metadata того же PID — один раз. Это reconnaissance, не сертификация Notepad.
10. Добавлен FI-001 shutdown test: источник, игнорирующий cancellation, освобождается coordinator-ом в bounded budget; общий набор тестов теперь 28/28.
11. Добавлен 30-секундный synthetic soak: 1,928 context pairs, 3,856 transitions, handle delta `+5`, thread delta `+1` после post-dispose GC. Это evidence для повторного сравнения, не leak-free certification.
12. Добавлен отдельный managed WPF fixture probe: отдельный дочерний процесс с пустым `TextBox`, результат `Pass`, UIA metadata `Edit`/`Wpf`, 0 text-reader calls и без чтения заголовка/текста.
13. Harness получил bounded параметр `--soak-seconds` с пределом 1800 секунд; отдельный 30-минутный запуск выполняется в `artifacts/phase1-long`, не смешивая baseline evidence.
14. Первый 30-минутный результат с `+43.9 MB` признан недействительным для leak-оценки: harness удерживал все наблюдения в `ConcurrentBag`. После замены на Interlocked-счётчики повторный run `Pass`: 1,800 секунд, 115,590 пар, 231,180 transitions, `+4.7 MB`, handles `+6`, threads `−2`, zero text/repository calls. В этом long-run managed WPF probe был `Inconclusive`, поэтому нужна повторяемость.
15. WPF fixture получил явную activation/topmost-последовательность. Пять коротких независимых повторов после исправления дали `5/5 Pass`, `Edit`/`Wpf`, минимум одно focused metadata observation и 0 text-reader calls. Long-run record до исправления сохранён как исторический `Inconclusive`; желательно повторить длинный прогон после fix.
16. Добавлен content-free `IntegrityCompatibility` (`Compatible|Incompatible|Unknown`): Windows integrity RID сравнивается без elevation и без target-content read; `Incompatible` coordinator переводит в `Unsupported`, а `Unknown` не получает положительного разрешения. Добавлены тесты current-process/invalid PID и coordinator fail-closed.
17. Harness получил отдельный observation-plane responsiveness record: 2000 пар событий, enqueue `1.43 ms`, `ui_thread_blocked=false`, zero text/repository calls. Это не подмена полноценного machine-level Avalonia UI latency evidence.
18. Завершён отдельный 60-секундный post-fix synthetic soak: `3,857` пар / `7,714` transitions, `Pass`, 0 text-reader/repository calls. WPF sample в этом запуске был `Inconclusive` (`Unknown` integrity); ранее зафиксированный независимый WPF repeat остаётся `5/5 Pass`. Попытка нового 1800-секундного прогона была остановлена лимитом среды до записи SOAK/WPF и не считается формальным результатом.
19. Добавлен реальный Desktop shell responsiveness probe: Avalonia старт `1118.97 ms`, `38/38` responding samples за 10 секунд, `0` non-responding, `ui_thread_blocked=false`, 0 text-reader/repository calls. Это закрывает shell-level наблюдение, но не заменяет длительный target/provider stress.
20. Добавлен автоматический WPF-only режим и repeat script. Чистый запуск после остановки основного приложения дал `0/5`: foreground transitions приходят, но focused metadata для WPF fixture не приходит. Summary намеренно `Inconclusive`; это реальный оставшийся blocker, а не повод засчитывать старую hand-written запись `5/5` как достаточную.
21. Проверены ShellExecute, видимость в taskbar, отложенная повторная активация и persistent Topmost для fixture; второй автоматический repeat также `0/5`. Launch/activation tweaks не закрыли focus-delivery проблему, поэтому дальше требуется отдельная UIA event-path диагностика.
22. Добавлены bounded diagnostics в UIA source: WPF probe показал `5` reconciliation signals, `5` non-null metadata reads, но последний focused PID — `csrss`; это отделяет рабочий metadata-read path от отсутствия интерактивного input focus в среде. Никакого target text или dynamic UIA string в диагностику не добавлялось.
23. Добавлен machine-readable `WP14-HANG` provider-stress record и unit test `HungProvider_DoesNotBlockBoundedDispose`; synthetic hung source release count `1`, coordinator dispose `1 ms`, общий набор тестов теперь `32/32`.
24. Добавлен `scripts/phase1_exit_gate.ps1`: формальная проверка Phase 1 теперь машинно подтверждает `7/8` критериев; единственный fail-closed blocker — `DR-PHASE1-0003` (automated WPF repeat `0/5`).
25. Проверен узкий test-only `SyntheticWindowActivator` (`SetForegroundWindow`) в `Platform.Windows`; третий automated repeat также `0/5`. Это подтверждает non-interactive environment blocker, а helper не используется production observation path.
26. Укреплён `scripts/phase1_exit_gate.ps1`: gate теперь выбирает самый свежий валидный `WP14-WPF-REPEAT-AUTO.json` среди всех versioned evidence-каталогов, фиксирует выбранный путь/число кандидатов, проверяет его `contentSafetyAudit` и отклоняет пустые/неполные repeat summaries (`repeat_count=0` или несовпадающее число attempts). Текущий выбор — `artifacts/phase1-wpf-repeat-activation-helper`, результат остаётся `Inconclusive`, `7/8`, `DR-PHASE1-0003`.
27. Проведён чистый WPF probe при остановленном Desktop shell и четвёртый независимый automated repeat: single probe получил `5` non-null metadata reads, но focused PID снова `csrss`; automated repeat — `0/5`. Gate обновлён и теперь выбирает `artifacts/phase1-wpf-repeat-current-auto` как самый свежий кандидат (`9` кандидатов всего), сохраняя `7/8` и `DR-PHASE1-0003`.
28. В UIA source добавлен content-free счётчик provider/read failures, а harness получил детерминированную диагностику доставки (`FixtureFocused`, `ProviderReadFailure`, `ProviderReturnedNull`, `MetadataReadButForeignFocus` и др.). Новый probe дал `MetadataReadButForeignFocus`: `5/5` non-null reads, `0` failures, fixture PID `12892`, focused PID `1076` (`csrss`). Добавлен unit test failure counter; общий набор тестов теперь `33/33`.
29. Harness дополнен раздельными foreground/focus counters. Новый probe зафиксировал WinEvent foreground fixture дважды (`target_foreground_observation_count=2`, PID `23128`), но UIA пять раз вернул другой focused PID `1076` (`csrss`), `0` provider failures и ту же `MetadataReadButForeignFocus`. Это формально локализует разрыв между исправным foreground observation и недоступным интерактивным UIA focus.
30. Добавлен `scripts/run_phase1_evidence_suite.ps1`: единый reproducible runner для build, WP-1.4 harness, Desktop UI responsiveness, WPF repeat и exit-gate. Он сохраняет step-by-step machine-readable summary в `artifacts/phase1-suite/PHASE1-EVIDENCE-SUITE.json` и явно отделяет ожидаемый WPF `Inconclusive` от реального сбоя шага.
31. Исправлен и проверен агрегатор suite: failure codes гарантированно сериализуются массивом, criteria считаются через `PSObject.Properties`, а smoke run (Notepad, 1s soak, 3s UI, 2 WPF repeats) завершился ожидаемо `Inconclusive` с `7/8`, четырьмя успешными шагами и `DR-PHASE1-0003`.
32. После ужесточения формального параметра до минимум 5 WPF повторов выполнен полный suite (Notepad, 30s soak, 10s UI, 5 WPF repeats): `104.0 s`, Harness/Desktop Pass, WPF `0/5`, gate `7/8`, `DR-PHASE1-0003`. Итоговый suite JSON корректен и выбран gate как самый свежий автоматический evidence.
33. Реализован bounded exponential backoff в `UiaFocusedElementMetadataSource` для повторных provider failures: 250 мс → максимум 5 с, без hot retry loop; добавлены `BackoffSuppressedCount` и unit test восстановления после сбоя. Общий набор тестов теперь `34/34`; privacy guards остаются зелёными.
34. Backoff вынесен в отдельный `ProviderFailureBackoff` reliability-компонент с monotonic clock, точной cap-последовательностью `250/500/1000/2000/4000/5000 ms` и reset-on-success. Добавлен детерминированный clock-driven unit test; общий набор тестов теперь `35/35`, production UIA source не получил новых content APIs.
35. Suite-runner расширен до полного verification pipeline: Debug/Release build, `35/35` tests, Phase 1 scope guard, Phase 2 content-boundary guard, WP-1.4 harness, Desktop UI, WPF repeat и exit-gate. Полный запуск с Notepad/30s soak/10s UI/5 WPF repeats занял `114.8 s`; семь required steps прошли, две ожидаемо inconclusive, итог gate `7/8`, `DR-PHASE1-0003`.
36. Проведён отдельный интерактивный WPF confirmation probe. После ручного открытия fixture окно дважды наблюдалось как foreground, UIA пять раз вернул структурные метаданные без provider failures, но focused PID оставался системным `csrss`; fixture-focused metadata не доставлена в bounded window. Результат `Inconclusive`, что подтверждает тот же environment-level focus-delivery blocker и не даёт оснований ослаблять formal gate.
37. Полный verification pipeline повторён после интерактивного probe и остановки stale dotnet-процессов: Debug/Release build без предупреждений и ошибок, `35/35` tests, оба privacy/scope guard, WP-1.4 harness и Desktop UI прошли; WPF repeat снова `0/5`, gate `7/8`, suite `Inconclusive`, длительность `114.4 s`. Контрольные inventory/hash-манифесты пересобраны: `529` файлов, `276` Markdown, `528` checksum entries.
38. Исправлен интерфейс `run_phase1_evidence_suite.ps1`: параметр `SummaryDirectory` получил совместимый alias `OutputDirectory`, поэтому вызовы из handoff-команд больше не silently пишут summary в другой каталог. Изменение не затрагивает evidence path, privacy boundary или exit-gate.
39. Alias проверен smoke-запуском с `-OutputDirectory artifacts\\phase1-suite-alias-smoke`: тесты `35/35`, оба guard и harness/Desktop UI прошли, ожидаемые WPF repeat и gate остались `Inconclusive` (`0/5`, `7/8`), summary записан именно в переданный каталог.
40. Для локализации WPF blocker добавлена test-only проверка активации окна: `ShowWindow(RESTORE)`, `BringWindowToTop`, `SetActiveWindow`, `SetForegroundWindow` с кратким `AttachThreadInput`, а также content-free telemetry попыток. Пятый automated repeat показал одинаковый результат во всех пяти попытках: `19` activation attempts, `0` successes, `activation_verified=false`, foreground fixture `2`, focused metadata `0`, `MetadataReadButForeignFocus`. Fallback не изменил production path и не ослабил gate.
41. После fallback выполнен полный suite: Debug/Release build без ошибок, `35/35` tests, оба guard, harness и Desktop UI прошли; WPF `0/5`, formal gate `7/8`, итог `Inconclusive`, длительность `127.4 s`. Gate выбрал самый свежий валидный summary `phase1-wpf-repeat-activation-telemetry`, всего кандидатов `11`.
42. Добавлен отдельный real-worker provider-stress режим (`--provider-stress`) и скрипт `run_provider_stress.ps1`. Чистый запуск выполнил `2000` reconciliation requests, получил `2001` bounded signals, `2` metadata reads/`2` non-null structural results, `0` provider failures, `0` backoff suppressions, `0` text/repository calls и завершился `Pass` за `345 ms`; channel disposal исправлен так, чтобы не оставлять `ReadAllAsync` iterator error.
43. Provider-stress включён в общий verification suite как отдельный required step. Финальный suite выполнил Debug/Release build, `35/35` tests, оба guard, provider stress `Pass`, WP-1.4 harness `Pass` и Desktop UI `Pass`; WPF repeat и exit-gate остались ожидаемо inconclusive. Итого `8/8` required steps, `2` expected inconclusive, `7/8` formal criteria, `128.6 s`.
44. Диагностический verdict WPF уточнён: при `19` test-only activation attempts, `0` успешных активаций и `activation_verified=false`, одновременно наблюдаются foreground fixture (`2`) и рабочие non-null UIA metadata reads (`5`, provider failures `0`), но fixture-focused metadata отсутствует. Новый стабильный diagnosis — `ActivationBlocked`; это исключает provider crash и локализует blocker на уровне интерактивной среды.
45. После введения `ActivationBlocked` полный suite повторён: `35/35` tests, Debug/Release build без ошибок, оба guard, provider stress, harness и Desktop UI прошли; все пять WPF attempts классифицированы как `ActivationBlocked`, repeat `0/5`, formal gate `7/8`, suite `8/8` required steps, длительность `129.9 s`.
46. UIA `SetFocus` ограничен одним bounded-attempt на fixture, чтобы provider-вызов не повторялся 19 раз и не растягивал repeat. Финальный suite после этого ограничения завершился предсказуемо за `138.8 s`: `35/35` tests, `8/8` required steps, provider stress/harness/Desktop UI Pass, WPF `0/5`, все attempts `ActivationBlocked`, formal gate `7/8`.
47. Exit-gate усилен: он агрегирует Win32/UIA activation telemetry и fail-closed отклоняет любой WPF `Pass`, если попытки помечены `ActivationBlocked`. Текущий selected summary даёт `5` blocked attempts, `93` Win32 attempts/`0` successes, `5` UIA focus calls и `0` foreground verifications; formal gate остаётся `7/8` с тем же единственным `DR-PHASE1-0003`.
48. После кратковременного инфраструктурного сбоя шага HARNESS выполнен повторный полный suite: Debug/Release build, `35/35` tests, оба guard, provider stress, harness и Desktop UI — Pass; WPF `0/5`, gate `7/8`, suite `8/8` required steps, `140.0 s`. Gate telemetry обновилась до `5` blocked attempts, `95` Win32 attempts/`0` successes и `5` UIA focus calls/`0` foreground verifications.
49. Оформлен отдельный `docs/PHASE1_PROVIDER_STRESS_REVIEW_2026-09-04.md`: provider-stress review принят как `Pass`, privacy/correctness invariants перечислены, воспроизводимые команды и evidence matrix собраны, а единственный blocker `DR-PHASE1-0003` явно оставлен fail-closed. Следующей нейронке не нужно повторно восстанавливать контекст WP-1.4 по разрозненным записям.
50. Добавлен `scripts/phase1_evidence_package_guard.ps1` и включён в общий suite после свежего WPF repeat. Guard проверяет 8 обязательных JSON, schema/privacy audit, provider-stress bounds, нулевые text-reader/repository calls и согласованность `ActivationBlocked` telemetry. Полный прогон завершён: `35/35` tests, `9/9` required steps, `2` ожидаемо inconclusive, WPF `0/5`, formal gate `7/8`, `DR-PHASE1-0003`, `130.5 s`.
51. Добавлен test-only `InteractiveSessionPreflight` в `DraftRescue.Platform.Windows` и wrapper `run_wpf_interactive_preflight.ps1`. Он фиксирует только session/window/focus handles и доступность input desktop, не читая title, text, clipboard или UIA values. Текущий preflight: `user_interactive=true`, `input_desktop_accessible=true`, но `foreground_window_present=false`, поэтому `ready_for_interactive_fixture=false`; это объясняет `ActivationBlocked` и оставляет gate fail-closed. Полный suite после regression test: `36/36` tests, `9/9` required steps, `128.5 s`, formal gate `7/8`.
52. Добавлен единый `scripts/run_wpf_interactive_review.ps1`: он сохраняет advisory preflight, затем в normal mode запускает bounded interactive probe и объединяет preflight/probe/privacy в один `WP14-WPF-INTERACTIVE-REVIEW.json`; `-PreflightOnly` smoke mode проверен без зависания на `Console.ReadLine`. Текущий review-record остаётся `Inconclusive` только потому, что probe намеренно не запускался в smoke mode; production gate не изменён.
53. Evidence-package guard расширен проверкой optional interactive-review record, а `docs/PHASE1_WPF_INTERACTIVE_RUNBOOK.md` описывает безопасный ручной сценарий: fixture остаётся пустым, пользователь только кликает поле и подтверждает Enter. Guard smoke повторно `Pass`; никаких content reads или clipboard/keyboard fallbacks не добавлено.
54. После расширения guard выполнен свежий полный suite: Debug/Release build без предупреждений и ошибок, `36/36` tests, provider stress, оба scope/privacy guard, harness, Desktop UI и evidence-package guard — `Pass`; WPF repeat и exit-gate ожидаемо inconclusive. Итог: `9/9` required steps, `2` expected inconclusive, formal gate `7/8`, `129.6 s`.
55. Interactive WPF record расширен preflight-снимками до и после ручного клика: только session/window/focus handles и input-desktop accessibility, без title/text/clipboard. Release harness и Debug solution пересобраны без предупреждений, `36/36` тестов прошли; свежий preflight снова `Inconclusive` (`foreground_window_present=false` до запуска fixture), gate не ослаблен.
56. Устранён инфраструктурный хвост suite: `run_phase1_wp14.ps1` переведён с `dotnet run` на прямой собранный Release `.exe`, а `run_phase1_evidence_suite.ps1` получил fail-safe cleanup только для процессов, созданных текущим запуском. Свежий полный suite: `36/36`, `9/9`, `128.5 s`, `7/8`; counters `cleaned=0`, `leaked=0`, после завершения `dotnet`-хвостов не осталось.
57. Добавлен отдельный `phase1_harness_process_guard.ps1` и включён в suite как required step. Он запускает изолированный direct Release harness, проверяет 6 обязательных записей и отсутствие дочерних `dotnet`/harness-процессов. Свежий suite: `36/36`, `10/10` required steps, `141.4 s`, `7/8`; process guard `Pass`, `cleaned=5`, `leaked=0`.
58. После наблюдения позднего появления `dotnet`-хостов telemetry suite усилена: добавлены baseline count, owned-process count до/после финального drain и единый безопасный enumerator только для `dotnet`/harness/test hosts. Полный suite повторён на чистом baseline: `36/36` тестов, `10/10` required steps, `142.3 s`, WPF `0/5`, formal gate `7/8` с единственным `DR-PHASE1-0003`; за текущий запуск очищено `15` принадлежащих suite процессов, после финального drain owned count `0`, leaks `0`. Никаких command-line или content diagnostics не добавлялось.
59. Андрей выполнил bounded интерактивный WPF confirmation: `Pass`, `FixtureFocused`, foreground observations `3`, focused metadata `1`, UIA metadata reads `18`, provider failures `0`, text-reader/repository calls `0`. Exit-gate обновлён fail-closed проверкой этого evidence-пути: автоматический repeat остаётся диагностическим `0/5`, но валидная интерактивная запись закрывает WPF focus-delivery criterion без ослабления privacy. Полный suite `artifacts/phase1-suite-final5` завершён `Pass`: `36/36` тестов, `11/11` required steps, formal gate `8/8`, один ожидаемый inconclusive diagnostic, cleanup `5`, owned-after-drain `0`, leaks `0`.
60. Начата Phase 2 и закрыт крупный WP-2.1..WP-2.6 блок: добавлены typed `SecuritySignal`/`SecurityEvidenceSet`, pure `SecurityPolicyEvaluator` с канонической deny precedence, synthetic positive predicate и stable `DR-SEC`/`DR-OBS` codes; добавлен opaque process-local `AllowedFieldHandle` с binding/generation/profile re-check, monotonic deadline `1000 ms` (hard cap `2000 ms`), atomic single-claim/revoke/expiry semantics. Добавлены `73/73` тестов (policy matrix, negative/unknown/private/credential/payment cases, architecture denylist, single-use/expiry/concurrency) и `scripts/run_phase2_security_gate.ps1`: matrix `24/24` (`23` negative, `1` positive), `0` content reads, `0` forbidden API violations. Phase 2 native fixture и финальный WP-2.7/2.8 review остаются следующими задачами; Phase 3 text reader не добавлялся.
61. Закрыт крупный WP-2.7 блок: добавлена test-only Windows Forms/UIA native secure-field fixture с тремя пустыми TextBox-контролами. Structural-only probe подтвердил ordinary editable `Allowed` с capability issue, password `SecureField` deny и read-only fail-closed deny; `content_read_invocations=0`. Полный `run_phase2_security_gate.ps1 -IncludeNativeFixture` прошёл: solution Debug build 0/0, harness Release build 0/0, tests `73/73`, boundary guard Pass, synthetic matrix `24/24`, native fixture `3/3`, forbidden APIs `0`. Остаётся только WP-2.8 — расширенный fault/negative evidence и финальный Phase 2 exit review; Phase 3 reader не добавлялся.
62. Закрыт WP-2.8 и весь Phase 2 gate: `PHASE2-FAULT-HARNESS.json` исполняет все 24 сценария (`24/24`, `23` negative + `1` positive), 10 000 deterministic allow/deny evaluations, 10 000 issue/revoke capability cycles без retained registry state, 128 deny-precedence permutations с одним результатом и concurrent duplicate claim с одним победителем. Исторический финальный gate `artifacts/phase2-security-gate-wp28/PHASE2-SECURITY-GATE.json` — `Pass`: build/harness `0/0`, tests `82/82` на границе Phase 2, native fixture `3/3`, fault harness `Pass`, boundary/API guard Pass, `0` forbidden APIs и `0` content reads. После начала Phase 3 повторный Phase-2 guard ожидаемо фиксирует наличие разрешённого reader и не является новым Phase-2 exit gate.
63. Начат Phase 3 и закрыт крупный bounded блок WP-3.1/WP-3.2/WP-3.4..WP-3.6: добавлены one-shot `IEligibleFieldTextReader` contract и typed result union, bounded `ReadBudget`, certified TextPattern adapter с finite `limit+1` и target re-check, transient exact-text `FieldTextSnapshot`, generation/sequence-safe `InMemoryDraftTracker` с одной текущей записью и 300 ms empty stabilization. Добавлены reader/snapshot/tracker/architecture/privacy tests; suite вырос до `95/95`. `run_phase3_application_gate.ps1` и content-boundary guard проходят, production reader implementations `1`, bounded TextPattern readers `1`, target-content reads `0`.
64. Закрыт следующий bounded блок WP-3.3: добавлен certified ValuePattern adapter для single-line surfaces. Он тратит capability до чтения, не имеет provider-side truncation, применяет post-read UTF-16 cap без обрезки, проверяет target generation и возвращает stable `DR-UIA-3002` без текста. Добавлены 10 focused tests; suite `105/105`. Phase 3 application gate Pass: два reader implementations, zero forbidden APIs и zero target-content reads.
65. Закрыт WP-3.7 integrated fault/non-leakage block: добавлены integrated canary/fault tests, проверяющие успешный поток обеих стратегий в один current tracker state, отсутствие persistence/protector/clipboard/network/UI-body/revision sinks, отсутствие mutations на oversize/failed/cancelled/stale путях и отсутствие canary в JSON/diagnostics. Новый `run_phase3_integrated_gate.ps1` проходит: build `0/0`, tests `112/112`, boundary guard Pass, forbidden APIs `0`.
66. Закрыт WP-3.8 и весь Phase 3: 30-minute operational soak прошёл `1800/1800 s`, `2,573,110,419` успешных bounded reads, один current tracker record, zero revision history, no unbounded growth и zero privacy sinks. Финальный `PHASE3-EXIT-GATE.json` — Pass с `phase3Exit=true`.
67. Начат Phase 4 и закрыт крупный WP4.1: добавлены `ProtectedDraftRecordV1`, ciphertext-only `IProtectedDraftRepository`, transient/protected payload boundaries, metadata-only recovery contract и canonical `SqliteSchemaV1` с `WITHOUT ROWID`, sequence/timestamp checks, expiry index и безопасными DELETE/secure_delete/EXTRA pragmas. Добавлены 9 контрактных/архитектурных тестов; suite `121/121`, contracts guard Pass, `PHASE4-CONTRACTS-GATE.json` Pass. SQLite runtime, DPAPI, installation secret и coordinator намеренно не включались; следующий bounded пакет — WP4.2.
68. Закрыт крупный WP4.2: добавлены строгий `DRP1` codec, `WindowsDpapiDraftProtector` с `DataProtectionScope.CurrentUser` и пустым entropy, fail-closed stable error codes, buffer zeroing и `WindowsDpapiInstallationSecretStore` с 256-битным случайным ключом/opaque versioned file. Добавлены 6 DPAPI/secret tests; suite `127/127`, DPAPI guard Pass, `PHASE4-DPAPI-GATE.json` Pass. Текущий test host не загрузил user profile для реального DPAPI round-trip, поэтому тесты фиксируют `DpapiFailure` fail-closed path без plaintext fallback; следующий bounded пакет — WP4.3 SQLite bootstrap/schema migration и serialized writer.
69. Закрыт крупный WP4.3: подключён `Microsoft.Data.Sqlite`, добавлены bootstrap/schema v1 с явными DELETE/secure_delete/foreign_keys/busy_timeout/EXTRA pragmas, unknown-version fail-closed, serialized `SqliteProtectedDraftRepository`, transactional monotonic upsert (`Inserted/Updated/IdempotentNoChange/StaleIgnored`), typed equal-sequence conflict, metadata-only listing, expiry/delete-all и concurrent writer semantics. Добавлены 6 SQLite tests; suite `133/133`, SQLite guard Pass, `PHASE4-SQLITE-GATE.json` Pass. Coordinator, retention, Preview, Copy и Restore не включались; следующий bounded пакет — WP4.4 coordinator/checkpoint state machine и retention execution.
70. Закрыт крупный WP4.4: добавлены `CheckpointCandidate`, protect-before-repository `PersistenceCheckpointCoordinator` с generation/cancellation/failure/idempotency outcomes и без plaintext checkpoint queue, а также `SqliteRetentionService` для metadata-only expiry cleanup. Добавлены 6 coordinator tests; suite `139/139`, coordinator guard Pass, `PHASE4-COORDINATOR-GATE.json` Pass. Preview, Copy и Restore не включались.
71. Закрыт крупный WP4.6: добавлены typed `Ready/Incompatible/Corrupt/Unavailable` store-open outcomes, локальный non-overwrite quarantine, известная-версия migration policy и typed malformed-record handling без salvage/auto-drop/network paths. Добавлены 3 recovery tests; suite `142/142`, recovery guard Pass, `PHASE4-RECOVERY-GATE.json` Pass. Следующий bounded пакет — WP4.7 plaintext-at-rest canary и crash/lock/disk-full certification; Phase 4 exit pending.
72. Закрыт крупный WP4.7: добавлены synthetic UTF-8/UTF-16 plaintext-at-rest canary scans после закрытия хранилища, 500-checkpoint one-row stress, pre-commit rollback verification, typed disk-full-equivalent failure и bounded fault gate. Suite `147/147`, plaintext/fault guard Pass, `PHASE4-FAULT-GATE.json` Pass. Следующий bounded пакет — WP4.8 final Phase 4 exit; Preview, Copy и Restore не включались.
73. Выполнен WP4.8 final exit review: единый combiner проверил все WP4.x artifacts, source boundary и suite `147/147`; отдельный content-free `PHASE4-DPAPI-RUNTIME-PROBE.json` повторяемо показал `available=false`, а реальный `PHASE4-PROCESS-KILL-PROBE.json` подтвердил rollback после убийства child process во время незавершённой транзакции. Результат `Inconclusive` по fail-closed правилу: положительный DPAPI CurrentUser roundtrip и real disk-full certification ещё не подтверждены. Артефакт `PHASE4-EXIT-GATE.json` создан; Preview, Copy и Restore не включались.
74. Добавлен `PHASE4-TARGET-ENVIRONMENT-PROBE.json`: профиль и временный том доступны для записи, но контролируемая quota/virtual-disk среда для реального disk-full injection отсутствует. Exit gate повторён: `147/147`, process-kill Pass, DPAPI и disk-full остаются единственными blockers.
75. Добавлена детерминированная `BeforeCommit` fault-injection точка в SQLite repository: synthetic disk-full отказ откатывает транзакцию и сохраняет старую protected row. Suite расширена до `148/148`; production fallback не добавлен.
76. DPAPI runtime probe переведён с reflection-loading на скомпилированный `DraftRescue.Phase4CrashProbe` с прямой ссылкой на Platform.Windows. Probe запускается воспроизводимо и возвращает typed `DpapiFailure` в текущем host; exit gate остаётся Inconclusive только по DPAPI и real disk-full.
77. Исправлена валидация аргументов crash-probe: режим `dpapi` больше не завершается до вызова API из-за отсутствующего пути БД. Повторный compiled probe действительно вызывает DPAPI и фиксирует `probeExitCode=6` / `DpapiFailure`; прежний ложный ранний `exit=2` устранён.
78. Выполнен крупный WP4.8 hardening-блок: target-environment probe переведён на schema v2 с content-free typed session/identity/profile/volume diagnostics и безопасным fallback для free-space; DPAPI runtime probe получил typed `harnessExitCode`/`executionStatus` и различает ожидаемый `DpapiFailure` от поломки harness; exit combiner переведён на schema v2 с динамическим разбором `148/148`, required `items[]`, `pendingItems[]`, честным `phase4Exit` и `syntheticEvidenceOnly`. Полный gate повторён: build `0/0`, tests `148/148`, process-kill Pass, итог `Inconclusive` с двумя прежними blockers (DPAPI CurrentUser roundtrip и real controlled disk-full). Для повторяемости gate удаляет только проверенный stale generated `DraftRescue.Desktop.dll` под `obj` перед сборкой; исходники и пользовательские данные не затрагиваются.
79. Подготовлен безопасный target-certification runbook: `scripts/run_phase4_target_certification.ps1` объединяет environment/DPAPI/process-kill probes и принимает внешний disk-full evidence только при `RealControlledDiskFull`, `Pass`, `syntheticOnly=false`, `containsSecrets=false`. Добавлен `docs/PHASE4_TARGET_CERTIFICATION_RUNBOOK.md` с требованиями интерактивного загруженного профиля и disposable quota/virtual-disk; автоматическое создание или заполнение VHD намеренно отсутствует.
80. Основной `run_phase4_exit_gate.ps1` интегрирован с target certification: добавлен явный `-DiskFullEvidencePath`, в итоговом JSON фиксируются `targetCertificationOutcome`, `targetCertificationPass` и безопасная метка evidence без абсолютного пользовательского пути, а real disk-full проходит только при typed `RealControlledDiskFull`/`Pass`. На текущем host результат остаётся Inconclusive, без ложного promotion.
81. Выполнена privacy-проверка внешнего evidence: synthetic fixture с `classification=RealControlledDiskFull`, `outcome=Pass`, но `syntheticOnly=true` был отклонён как `Fail` с отдельным blocker `real-disk-full-evidence-rejected-by-typed-privacy-contract`; после теста основной артефакт восстановлен в canonical `Inconclusive`, без сохранения временного пути.
82. Для WP4.8 cleanup/evidence hardening зафиксированы инварианты P-008, P-019, P-020, P-030, P-046 и C-021/C-049: никаких draft/secret/path leaks в diagnostics, только synthetic/non-sensitive evidence до target certification, typed rejection и воспроизводимый bounded gate; новые process-cleanup counters показывают `leakedCount=0`.
83. Добавлен `phase4_artifact_privacy_guard.ps1`, подключённый к exit-gate: он проверяет все текущие Phase-4 JSON (8 файлов) на запрещённые content-поля, absolute path values, `containsSecrets=true` и malformed JSON. Последний guard — `Pass`, `rawContentExported=false`, `containsSecrets=false`; process cleanup остаётся bounded по baseline PID/time.
84. Typed privacy contract усилен против null-coercion: `RealControlledDiskFull` evidence без явных `syntheticOnly` и `containsSecrets` теперь отклоняется. Negative fixture с отсутствующими полями дал exit-gate `Fail`, затем canonical gate восстановлен в `Inconclusive`; текущий guard — `Pass`, findings `0`.
85. Проведён полный WP4 regression: `run_phase4_contracts_gate.ps1`, `run_phase4_dpapi_gate.ps1`, `run_phase4_sqlite_gate.ps1`, `run_phase4_coordinator_gate.ps1`, `run_phase4_recovery_gate.ps1` и `run_phase4_fault_gate.ps1` — все `Pass`, каждый с `148/148`; итоговый exit gate — `Inconclusive` только по DPAPI CurrentUser и real disk-full. Свежие безоконные `dotnet`-процессы после прогона очищены.
86. Уточнена диагностика профиля: environment probe теперь различает `Loaded`, `NotLoaded`, `Unknown` и `QueryUnavailable` через typed `loadState`/`loadStateQueryAvailable`. На текущем host зафиксировано `QueryUnavailable`, поэтому DPAPI readiness не повышается; неопределённость остаётся fail-closed.
87. Runner target certification больше не падает на отсутствующем/битом external disk-full JSON: выдаёт typed `Inconclusive`, `diskFullEvidenceLoadStatus=DiskFullEvidenceNotFound|DiskFullEvidenceMalformed` и сохраняет следующий шаг. Canonical run без внешнего файла остаётся `NotSupplied`; artifact privacy guard — `Pass`.
88. Добавлен `phase4_handoff_consistency_guard.ps1`: он сверяет `CODEX_HANDOFF_STATUS.json`, Phase-4 exit/target/privacy artifacts и inventory/checksum counts. Последний запуск — `Pass`, findings `0`, согласованы `1420` файлов и `294` Markdown.
89. Добавлены bounded `PersistenceCheckpointScheduler` и `PersistenceCheckpointWorker`: явная политика debounce/max-dirty-age/retry, coalescing по `DraftId`, newest-candidate selection, single-flight execution и bounded one-retry budget без plaintext queue. Runtime владеет worker-ом и корректно освобождает его до coordinator/SQLite.
90. Диагностика DPAPI стала структурной: `DraftProtectionFailureReason` (`PlatformNotSupported`, `Unauthorized`, `Cryptographic`, `Unknown`) передаётся через платформенный слой и сохраняется только как закрытая категория, без текста исключения.
91. Скомпилированный DPAPI probe v3 проверяет оба CurrentUser пути — payload и installation HMAC secret — и публикует только boolean round-trip, `failureStage`, `failureReason` и закрытый `readiness.blockers`. На текущем host оба round-trip не наблюдаются; `harnessExitCode=0`, то есть это повторяемый target result, а не поломка harness.
92. Все Phase-4 runners получили bounded MSBuild/process-scope настройку (`MSBUILDDISABLENODEREUSE=1`) и сохраняют только структурные cleanup counters. Документация уточнена: настройка снижает cross-run contamination risk, но не подменяет реальную target certification.
93. Readiness hardening завершён: DPAPI probe различает `Loaded`, `NotLoaded`, `Unknown` и `QueryUnavailable`, а неизвестные profile-state значения получают `profile-state-unrecognized`. Artifact privacy guard проверяет закрытые readiness status/blocker labels и отклоняет `Pass` с непустым blocker list; текущий guard проверил `2` readiness contracts и дал `Pass`. Handoff guard теперь требует эту coverage перед собственным `Pass`.
94. Выполнена полная prior-gate regression-проверка: runners WP4.1/WP4.2/WP4.3/WP4.4/WP4.6/WP4.7 повторно прошли с актуальным suite `181/181`, все дали `Pass`. Исторические scope/closure counts сохранены, а aggregate handoff дополнен отдельным `priorGateRegressionTestSuite=181/181`.

## Проверка результата

- Phase 1 scope guard: passed.
- `dotnet restore .\\DraftRescue.sln`: passed.
- `dotnet build .\\DraftRescue.sln --no-restore -c Debug`: passed after stopping stale build/test processes, 0 warnings, 0 errors (including Avalonia Desktop).
- `dotnet build .\\tests\\DraftRescue.Tests\\DraftRescue.Tests.csproj --no-restore -c Debug`: Phase-4 application/infrastructure/test build passed, 0 warnings/0 errors.
- `dotnet test .\\tests\\DraftRescue.Tests\\DraftRescue.Tests.csproj --no-build -c Debug`: latest verification passed, 181/181.
- `scripts/run_phase4_exit_gate.ps1`: latest WP4.8 review completed with build `0/0`, tests `181/181`, process-kill Pass, typed DPAPI result `DpapiFailure` (`failureStage=Protect`, `failureReason=Cryptographic`, `harnessExitCode=0`), target environment probe Pass, aggregate `Inconclusive` with exactly two pending items.
- `scripts/run_phase4_target_certification.ps1`: target-host runner remains `Inconclusive` on the current host because profile load state is `QueryUnavailable`, both DPAPI round-trips are unobserved, and no real disk-full evidence is supplied.
- `scripts/phase1_scope_guard.ps1` and `scripts/phase2_content_boundary_guard.ps1`: passed.
- Phase 2 historical gate `artifacts/phase2-security-gate-wp28/PHASE2-SECURITY-GATE.json`: Pass; solution/harness build `0/0`, tests `82/82` at the Phase-2 boundary, native fixture `3/3`, executable fault harness `24/24`, `10,000` stress evaluations and capability cycles, `0` forbidden APIs, `0` content reads.
- Phase 3 application gate `artifacts/phase3-application-gate-final/PHASE3-APPLICATION-GATE.json`: Pass for WP-3.1..WP-3.6; tracker/current-state, exact-text, bounded TextPattern and certified ValuePattern tests pass, guard reports two readers, zero forbidden APIs and zero content reads.
- Phase 3 integrated/exit gates `artifacts/phase3-integrated-gate-final/PHASE3-INTEGRATED-GATE.json` and `artifacts/phase3-exit-gate-final/PHASE3-EXIT-GATE.json`: Pass; operational soak `artifacts/phase3-soak-final/PHASE3-OPERATIONAL-SOAK.json` completed 1800 seconds with 2.57B successful iterations and no unbounded growth.

Применённые инварианты WP-1.3: P-001, P-002, P-003, P-025, P-026, P-028, P-029, P-030; C-003, C-016, C-017, C-018, C-022, C-023, C-025.

Для текущего WP4.8 cleanup/evidence hardening применяются P-008, P-018, P-030, P-043, P-046 и C-050: диагностика остаётся content-free, защита выполняется до persistence, неизвестное состояние target/profile не повышает readiness, а внешнее disk-full evidence принимается только по typed privacy contract.

Старый Phase 0 scope guard намеренно запрещает ссылку на `System.Windows.Automation`; после перехода к WP-1.2 это ожидаемое ограничение устаревшей Phase 0 проверки, а не основание ослаблять privacy guard. Для текущего этапа используется отдельный `scripts/phase1_scope_guard.ps1`. Полный `scripts/verify.ps1` поэтому всё ещё останавливается на историческом Phase 0 guard; актуальные Phase 1/2 guards проходят.

## Что остаётся сделать

WP-1.4 и Phase 1 evidence gate завершены. Phase 2 WP-2.1..WP-2.8 и Phase 3
WP-3.1..WP-3.8 полностью закрыты. Phase 4 WP4.1 contracts/schema gate также закрыт.

1. Завершить WP4.8 на target Windows profile: получить успешный DPAPI CurrentUser roundtrip и выполнить OS-level kill/real disk-full certification; затем повторить exit gate. Preview, Copy и Restore не включать до Pass.

## Важные замечания для продолжения

- Не добавлять keyboard hooks, Raw Input, clipboard fallbacks, generic LegacyIAccessible text fallback или логирование UIA values.
- Не переносить Win32/UIA типы в Domain/Application; платформенный код остаётся в `DraftRescue.Platform.Windows`.
- `AutomationIdToken` и `ClassNameToken` сейчас только bounded structural metadata, не логируются и не сохраняются. Перед использованием в persistence или диагностике потребуется отдельная privacy-проверка; сырые динамические UIA строки нельзя выводить в логи.
- `UiaCapabilityHints.LegacyPatternAvailable` зарезервирован для будущего certified adapter и не означает разрешение читать LegacyIAccessible content.
- Требуется сохранить текущую последовательность наблюдений, bounded channel и single-flight/коалесcing решения при реализации WP-1.4 и последующих адаптеров.

## Итог

WP-1.4 synthetic harness, Notepad, corrected 30-minute soak, integrity-boundary
implementation/tests, observation-plane responsiveness, Avalonia shell
responsiveness, real-worker provider stress и интерактивное WPF focus-delivery
подтверждение технически подтверждены; Phase 1 gate закрыт `8/8`.
Phase 2 WP-2.1..WP-2.8 подтверждены полным gate: Debug/Release build `0/0`,
`82/82` boundary tests at the Phase-2 close, synthetic matrix `24/24`, native fixture `3/3`, executable
fault harness `24/24`, boundary/API guard Pass и `0` content reads.
Phase 3 WP-3.1..WP-3.8 application/integrated/operational gate также Pass;
TextPattern и ValuePattern readers реализованы, non-leakage evidence собрана,
30-minute soak завершён, финальный exit review Pass.
Phase 4 WP4.1..WP4.7 gates Pass и WP4.8 hardening завершён: build `0/0`,
`181/181` tests, artifact privacy и handoff guards Pass. Final exit остаётся
`Inconclusive` fail-closed до успешного DPAPI CurrentUser roundtrip и real
controlled disk-full evidence; Phase 5 Preview/Copy/Restore не начинались.
